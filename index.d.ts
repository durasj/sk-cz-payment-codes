/**
 * Generate "reference message" from vs, ss, ks codes
 *
 * All arguments are optional. Format can be 'SK' or 'CZ', defaults to 'SK'.
 */
export declare function generate(vs?: string, ss?: string, ks?: string, format?: string): string;
